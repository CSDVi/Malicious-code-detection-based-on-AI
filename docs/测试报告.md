# 测试报告

## 单元测试

执行命令：
```powershell
cd D:\Z_TEMP_photo\AI\code\backend
python -m pytest -q
```

当前结果：5 项通过。覆盖用例包括 SQL 注入识别、WebShell 高危判定、安全代码低风险判定、TXT 文件检测、项目扫描摘要和高危文件排序。

## 模型评测

执行命令：
```powershell
cd D:\Z_TEMP_photo\AI\code\backend
python -m attack_detection.trainer --model rf
```

当前样例数据集规模为 75 条，覆盖 SQL 注入、XSS、WebShell、命令执行、SSRF、路径穿越、反序列化、敏感信息泄露、远程下载和混淆逃逸等风险类型。评测指标写入 `models/metrics.json`，页面 `/attack/evaluation` 可展示准确率、精确率、召回率、F1、误报率和混淆矩阵。

当前随机森林模型评测摘要：
- 准确率：0.8696
- 精确率：0.8333
- 召回率：1.0
- F1：0.9091
- 误报率：0.375

## 应用加载检查

执行命令：
```powershell
cd D:\Z_TEMP_photo\AI\code\backend
python -m compileall .
```

当前应用可正常编译，核心路由和检测模块加载成功。
